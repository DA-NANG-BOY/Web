export default function database() {
       this.username = new Array()  ;
       this.password = new Array() ;
       this.email =  new Array();
};


const fs = require('fs');

(async() => {
    
});